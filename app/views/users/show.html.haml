.block#block-tables
  .content
    %h2.title= "User: #{h @user.name}"
    .inner
      = render("navigation", :active => "show")
      .content
        .inner
          .left
            %h3 Admin
            = @user.admin

